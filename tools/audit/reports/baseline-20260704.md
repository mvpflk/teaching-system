# UI 一致性基线审计报告

**日期**: 2026-07-04
**模式**: 基线（首次全面扫描）
**扫描范围**: 341 个 .vue 文件（views/ + components/）

---

## 1. 硬编码颜色

| 指标 | 数值 |
|------|------|
| 总颜色引用次数 | 1209 |
| 独特颜色种类 | 262 |
| 已映射（有 CSS 变量） | 约 420+ |
| Element Plus 内部色（不动） | 约 350+ |
| 孤儿色（需决策） | 约 90+ |
| AI 模式色 | 约 30+ |
| 未知色（需人工确认） | 约 300+ |

### Top 30 高频颜色

```
145 × #4361ee → mapped:--primary-color
 49 × #f56c6c → ep_internal:Element Plus 默认危险色
 48 × #67c23a → ep_internal:Element Plus 默认成功色
 43 × #e6a23c → ep_internal:Element Plus 默认警告色
 43 × #86868b → mapped:--text-secondary
 42 × #909399 → ep_internal:Element Plus text-placeholder
 33 × #1d1d1f → mapped:--text-primary
 28 × #2e7d32 → mapped:--el-color-success
 27 × #f5f7fa → ep_internal:Element Plus fill-light
 27 × #e8e8ed → skeleton:--skeleton-bg
 26 × #eef0ff → mapped:--primary-light
 23 × #ed6c02 → mapped:--el-color-warning
 20 × #409eff → orphan:--primary-color
 19 × #bcbcc0 → mapped:--text-disabled
 14 × #ebeef5 → ep_internal:Element Plus fill
 14 × #e0e0e0 → skeleton:--skeleton-bg
 13 × #d32f2f → mapped:--el-color-danger
 13 × #303133 → ep_internal:Element Plus text-primary
 12 × #f39c12 → orphan:--el-color-warning
 12 × #f0f0f2 → mapped:--bg-secondary
 12 × #f0f0f0 → skeleton:--skeleton-highlight
 12 × #e74c3c → orphan:--el-color-danger
 11 × #e8f0fe → orphan:--primary-light
 10 × #fef0f0 → orphan:--bg-danger-light
 10 × #f59e0b → orphan:--el-color-warning
 10 × #e4e7ed → ep_internal:Element Plus border-light
  9 × #fafafa → mapped:--bg-hover
  9 × #00b42a → orphan:--el-color-success
  8 × #f6ffed → orphan:--bg-success-light
  8 × #f5f5f5 → skeleton:--skeleton-highlight
```

## 2. 移动端适配

| 状态 | 数量 |
|------|------|
| 总目录数 | 29 |
| 有移动端适配（isMobile 或 @media） | 28 |
| 无移动端适配 | **1** |

### 需适配的目录

- `checkpoint/` — 6 个文件，完全无 isMobile 和 @media

### 已使用 @media 但未使用 isMobile 的目录（桌面→移动端响应式已可通过全局 CSS 工作）

`ai-exam/`, `common/`, `error/`, `inspector/`, `login/`, `notification/`, `parent/`, `profile/`, `quality/`, `settings/`, `showcase/`, `simulation/`, `student/`, `system/`, `teacher/`, `typing/`, `wrong/`

这些目录的页面依赖全局 `index.scss` 中的响应式规则（768px 断点），基本能正常显示，但缺少 `isMobile` 驱动的模板级适配（如隐藏桌面端装饰元素、切换卡片/表格视图）。

## 3. 内联样式

| 类型 | 数量 |
|------|------|
| 内联 `style="color:..."` | ~200 处 |
| 内联 `style="margin/padding:..."` | ~434 处 |
| 内联 `style="display:flex"` | ~54 处 |

## 4. 渐变

| 类型 | 数量 |
|------|------|
| 总渐变 | ~64 |
| 紫色渐变（主色→紫色） | 3（CheckpointBoss.vue） |
| 青色渐变（主色→青色） | 2（CheckpointMixed, ClassroomIdleConfig） |

## 5. AI 模式

| 类型 | 状态 |
|------|------|
| 紫色残留 | 8+ 处（CheckpointBoss, PrecisionHub, BbsPostList 等） |
| AI 文案 | 1 处（Login.vue "欢迎回来"） |
| 装饰性图案 | 0 处 |

---

## 总体评估

```
维度            状态          优先级
────────────────────────────────────────
硬编码颜色      ⚠️ 大量      P0（逐步替换）
AI 模式残留     🚩 8+处      P1（逐个清理）
移动端适配      ✅ 框架完整   P2（checkpoint 需补充）
内联样式        ⚠️ 688处     P2（逐步迁移到工具类）
渐变规范        ⚠️ 5处问题   P1（紫色→品牌色）
```

## 建议行动

1. **P0**: 将 20 处 `#409eff` → `--primary-color`（影响面小，收益大）
2. **P1**: 清理 8 处紫色残留（CheckpointBoss, PrecisionHub 等）
3. **P1**: 替换 "欢迎回来" → 纯标题 "登录"
4. **P2**: 补齐 checkpoint/ 的移动端适配
5. **P2**: 分批替换内联颜色/间距为 CSS 变量/工具类

参见: `docs/superpowers/specs/ui-color-mapping.md`
